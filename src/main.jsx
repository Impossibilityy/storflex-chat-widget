import React from 'react'
import ReactDOM from 'react-dom/client'
import StorflexAssistant from './ChatWidget'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StorflexAssistant />
  </React.StrictMode>
)